# Cloudflare 域名配置（填写完整的子域名，例如 sub.example.com）
FULL_DOMAIN="your_subdomain.example.com"

# 网络接口名称（根据系统查询，如 eth0/ppp0）
NIC="ppp0"

# Cloudflare API 令牌（需具备 DNS 写入权限）
CF_TOKEN="your_cloudflare_api_token"

# Bark 通知 Token（从 Bark 客户端获取）
BARK_TOKEN="xxx"

# Telegram Bot 配置
# BotFather 提供的 Token
TG_BOT_TOKEN="123456:abc"
# 用户/群组 Chat ID
TG_CHAT_ID="your_chat_id"

# 服务标识名称（用于通知分组，例如 HomeServer）
SRV_NAME="MyServer"

######################
# HTTPS 记录高级配置（可选）
######################
# ALPN 协议（例如 h2,h3）
HTTPS_ALPN="h2,h3"

# 端口号（默认 443）
HTTPS_PORT="443"

# ECH 配置（需遵循 Cloudflare 格式）
ECH=""
